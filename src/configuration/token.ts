export const TokenConfig = {
    secret: 'jus_derein_jus_dawn',
    expiresIn: 60 * 60 * 8, //seconds

    renewTokenSecret: 'blood_must_have_blood',
    renewExpiresIn: 60 * 60 * 24 * 10, //seconds

    algorithm: 'RS256',
}